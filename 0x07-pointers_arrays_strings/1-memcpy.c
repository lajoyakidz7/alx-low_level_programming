#include <stdio.h>
/**
 * simple_print_buffer - Prints buffer in hexa
 * @buffer: the address of memory to print
 * @size: the size of the memory to print
 *
 *  Return: nothing.
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{ 
	unsigned int i:

		i = 0;
	while (i < size)
	{
		if ( i  % 10)
		{
			Printf(" ");
		} 
		if (!(i % 10) && I)
		{
			Printf("\n");
		}
		Printf("0x%02", buffer[i]);
		i++
	}
return: (0)
}
